# Xcode user-specific state
xcuserdata/
*.xcuserstate
DerivedData/
build/

# Editor / tooling backups
*.backup

# Local scratch / working dir (pitch-deck render scripts live in pitch-deck/scripts/)
scratch_pd/

# macOS
.DS_Store

# Local credentials — never commit
Config/Secrets.xcconfig
